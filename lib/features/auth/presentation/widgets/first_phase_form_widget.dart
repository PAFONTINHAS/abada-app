import 'package:flutter/material.dart';

class FirstPhaseFormWidget extends StatelessWidget {
  const FirstPhaseFormWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}